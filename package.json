{
  "name": "moves-awful",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check canvas-animations/arc.js && node --check canvas-animations/spiral.js && node --check scripts/install-vendor-skills.mjs && node --check tests/canvas-lifecycle.test.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "skills:list": "node scripts/install-vendor-skills.mjs --list",
    "skills:install": "node scripts/install-vendor-skills.mjs"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
