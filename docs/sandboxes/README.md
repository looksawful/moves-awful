# External sandboxes

MOVES AWFUL uses external sandboxes for runtime inspection, not as canonical source.

Canonical source remains this repository. Any Replit/CodePen copy must record the exact source commit it mirrors and must not become the only location of fixes.

## Replit

Creation was attempted on 2026-09-10 for the current MOVES AWFUL baseline. Replit rejected app creation with `requires_active_subscription`, so no Replit mirror currently exists.

When account access permits creation, mirror the current repository without converting the Vanilla Canvas runtime to React. Record the source SHA in the sandbox description and run the same syntax/build checks where supported.

## CodePen

No CodePen connector is available in the current ChatGPT plugin directory. No authenticated Pen was created during this pass.

A future CodePen mirror should be treated as a minimal browser demo rather than a replacement for Vite/package verification. If asset hosting or module URLs differ from the repository, document those sandbox-only adaptations explicitly.
